services:
  - type: web
    name: vk-anonymous-bot
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python bot.py"
    envVars:
      - key: VK_TOKEN
        sync: false
      - key: ADMIN_ID
        sync: false